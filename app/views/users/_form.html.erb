<%= form_for(@user, :html => { :class => "form-signin"} ) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation alert alert-danger">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <h2 class="form-signin-heading">Please sign in</h2>
  <%= f.label :login, 'Username', class: '' %><br>
  <%= f.text_field :login, class: 'form-control', value: '553290699' %><br>
  <%= f.label :password, 'Password', class: '' %><br>
  <%= f.password_field :password, class: 'form-control', value: 'NO-PASSWD' %><br>

  <%= f.submit 'Submit', class: 'btn btn-lg btn-primary btn-block' %>

<% end %>
